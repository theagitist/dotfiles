# Dotfiles management
alias dot='git --git-dir=$HOME/.dotfiles --work-tree=$HOME'

# General aliases
alias zipthis='f() { zip -r "${1:-$(basename $PWD)}.zip" .; }; f'
alias whatsmypublicipv4="dig -4 TXT +short +time=5 +tries=1 o-o.myaddr.l.google.com @ns1.google.com | tr -d '\"'"
alias whatsmypublicipv6="curl --max-time 5 https://get.geojs.io/v1/ip"

# Changing "ls" to "eza"
if command -v eza &>/dev/null; then
    alias la='eza -al --color=always --group-directories-first'
    alias ls='eza -a --color=always --group-directories-first'
    alias ll='eza -l --color=always --group-directories-first'
    alias lt='eza -aT --color=always --group-directories-first'
fi

# Make viu use always unicode block characters
alias viu='viu --blocks'

# Navigation
alias ..='cd ..'
alias ...='cd ../..'
mkcd() { mkdir -p "$1" && cd "$1" }

# Grep
alias grep='grep --color=auto'

# Quick edit configs
alias zshrc='$EDITOR ~/.zshrc'
alias vimrc='$EDITOR ~/.vimrc'
alias gitconf='$EDITOR ~/.gitconfig'
alias aliases='$EDITOR ~/.aliases'
alias tmuxconf='$EDITOR ~/.tmux.conf'

# Disk usage
if command -v duf &>/dev/null; then
    alias df='duf'
fi

# System info
alias ports="sudo lsof -i -P -n | grep LISTEN"

# Git
alias lg='lazygit'

# curlie (nicer curl output)
if command -v curlie &>/dev/null; then
    alias curl='curlie'
fi

# Reload shell
alias reload="exec zsh"

# fd: fdfind on Debian/Ubuntu, fd on macOS/other
if command -v fdfind &>/dev/null && ! command -v fd &>/dev/null; then
    alias fd='fdfind'
fi

# Changing "cat" to "bat" (batcat on Debian/Ubuntu, bat on macOS/other)
if command -v batcat &>/dev/null; then
    alias cat='batcat -p'
    alias ccat='batcat'
elif command -v bat &>/dev/null; then
    alias cat='bat -p'
    alias ccat='bat'
fi

